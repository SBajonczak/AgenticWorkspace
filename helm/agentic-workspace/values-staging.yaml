namespace: agentic-staging

config:
  nodeEnv: production

web:
  ingress:
    enabled: true
    host: workplace-staging.realapps.org
    tls:
      enabled: true
      secretName: workplace-staging-realapps-org-tls
      issuer: letsencrypt-staging

worker:
  replicaCount: 1