namespace: agentic-production

config:
  nodeEnv: production

web:
  ingress:
    enabled: true
    host: workplace.realapps.org
    tls:
      enabled: true
      secretName: workplace-realapps-org-tls
      issuer: letsencrypt-prod

worker:
  replicaCount: 1